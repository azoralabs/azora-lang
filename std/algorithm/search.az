/*
 * Copyright 2026 AzoraLabs
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

/**
 * @file std/algorithm/search
 *
 * Search functions for arrays.
 *
 * Provides linear search, binary search, min/max finding,
 * occurrence counting, and index-of-predicate for arrays.
 *
 * @since 0.1
 */
@Supress(.Unused)
module std.algorithm.search

import std.container.array::*

/**
 *
 */
@Since("0.1")
error SearchError {
    NotFound
    EmptyArray
}

/**
 * Performs a linear search for a target in an array.
 *
 * @param arr The array to search.
 * @param target The element to find.
 * @return The index of the first occurrence, or -1 if not found.
 */
@Experimental(since: "0.1")
func<T: Order> linearSearch(arr: Array<T>, target: T): Int {
    return for i: Int in 0..<arr.size { if arr[i] == target then break i } else { -1 }
}

/**
 * Performs a binary search on a sorted array.
 *
 * The array must be sorted in ascending order for correct results.
 *
 * @param arr The sorted array to search.
 * @param target The element to find.
 * @return The index of the target, or -1 if not found.
 */
@Experimental(since: "0.1")
func<T: Order> binarySearch(arr: Array<T>, target: T): Int {
    var {lo, hi}: Int = {0, arr.size - 1}
    while lo <= hi {
        fin mid = (lo + hi) / 2
        if arr[mid] == target then return mid
        if arr[mid] < target then lo = mid + 1 else hi = mid - 1
    }
    return -1
}

/**
 * Returns the minimum element in an array.
 *
 * @param arr The array to search (must be non-empty).
 * @return The smallest element.
 */
@Experimental(since: "0.1")
func<T: Order> minOf(arr: Array<T>): T
in {
    assert arr.size > 0 panic "Input must not be empty"
} scope {
    var result = arr[0]
    for i: Int in 1..<arr.size then if arr[i] < result then result = arr[i]
    return result
}

/**
 * Returns the maximum element in an array.
 *
 * @param arr The array to search (must be non-empty).
 * @return The largest element.
 */
@Experimental(since: "0.1")
func<T: Order> maxOf(arr: Array<T>): T
in {
    assert arr.size > 0 panic "Input must not be empty"
} scope {
    var result = arr[0]
    for i: Int in 1..<arr.size then if arr[i] > result then result = arr[i]
    return result
}

/**
 * Counts the occurrences of a target in an array.
 *
 * @param arr The array to search.
 * @param target The element to count.
 * @return The number of times target appears in the array.
 */
@Experimental(since: "0.1")
func<T: Order> count(arr: Array<T>, target: T): Int
out {
    assert it >= 0 panic "count must return non-negative"
} scope {
    var c: Int = 0
    for i: Int in 0..<arr.size then if arr[i] == target then c++
    return c
}

/**
 * Returns the index of the first element satisfying a predicate.
 *
 * @param arr The array to search.
 * @param pred The predicate function.
 * @return The index of the first match, or -1 if none found.
 */
@Experimental(since: "0.1")
func<T, I: Indexed<T>> findIndex(arr: Array<T>, pred: (T) -> Bool): Int {
    return for i: Int in 0..<arr.size { if pred(arr[i]) then break i } else -1
}
/*@Experimental(since: "0.1")
func<T, C: Index<T>> findIndex(collection: C, pred: (T) -> Bool): Int {
    return for i: Int in 0..<collection.size { if pred(collection[i]) then break i } else -1
}*/

/**
 * Returns the index of the last element satisfying a predicate.
 *
 * @param arr The array to search.
 * @param pred The predicate function.
 * @return The index of the last match, or -1 if none found.
 */
@Experimental(since: "0.1")
func<T> findLastIndex(arr: Array<T>, pred: (T) -> Bool): Int {
    return for i: Int in arr.size>..0 { if pred(arr[i]) then break i } else -1
}

/**
 * Checks if an array contains the given element.
 *
 * @param arr The array to search.
 * @param target The element to look for.
 * @return true if the element is found.
 */
@Experimental(since: "0.1")
func<T: Order> arrayContains(arr: Array<T>, target: T): Bool {
    return linearSearch<T>(arr, target) >= 0
}
